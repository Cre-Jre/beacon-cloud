# 服务名称
spring:
  application:
    name: beacon-strategy
  # 多环境
  profiles:
    active: dev
  # nacos注册中心地址
  cloud:
    nacos:
      discovery:
        server-addr: 101.35.130.68:8848
      # nacos配置中心地址:
      config:
        server-addr: 101.35.130.68:8848
        file-extension: yml
        # beacon-strategy-dev.yml
# ===========================Nacos配置中心=======================================
# 端口号
server:
  port: 10003

